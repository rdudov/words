[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
markers =
    e2e: End-to-end tests using real external services (OpenAI API, etc.)
addopts =
    -v
    --cov=src/words
    --cov-report=html
    --cov-report=term-missing
